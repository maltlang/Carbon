import { VirtualMachineContext, OpCode } from "./context"


export function
loadImages (vmContext: VirtualMachineContext, path: string) {
  // 留给月月姐姐写的function
}


export function wcToOpCode(input: string): OpCode[] | void {
  
} 