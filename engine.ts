import { VirtualMachineContext, OpCode } from "./context"
import { MValue } from "./mvalue"


function run(vmContext: VirtualMachineContext, opcode: OpCode): MValue | void {
    if (opcode.code == load_const) {
        
    }
    null
}